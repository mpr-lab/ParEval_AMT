[
    {
        "problem_type": "locking_contention",
        "language": "python",
        "name": "60_locking_contention_hist_freq",
        "parallelism_model": "charm4py",
        "prompt": "import charm4py\n\nusing histogram_t = std::array<std::uint64_t, 256>;\n\n    std::array<std::mutex, 256> locks;\n\n    SharedHistogram()\n    {\n        bins.fill(0);\n    }\n\n    def reset() -> None:\n        for (std::size_t i = 0; i < bins.size(); ++i)\n        {\n            std::lock_guard<std::mutex> g(locks[i]);\n            bins[i] = 0;\n        }\n    }\n\n    def increment(value: Any) -> None:\n        std::lock_guard<std::mutex> g(locks[value]);\n        ++bins[value];\n    }\n\n    def snapshot() -> Any:\n        histogram_t copy;\n        for (std::size_t i = 0; i < bins.size(); ++i)\n        {\n            std::lock_guard<std::mutex> g(locks[i]);\n            copy[i] = bins[i];\n        }\n        return copy;\n    }\n};\n   Build a 256-bin byte-frequency histogram from `data` (values in [0,255]) and return the resulting SharedHistogram object.\n   Use charm4py to compute in parallel.  Assume charm4py has already been initialized.\n   Example:\n      data = [0x20, 0x41, 0x20, 0x0A]\n      bins  = 256\n   output histogram[0x20] == 2, histogram[0x41] == 1, histogram[0x0A] == 1\n\ndef parallel_histogram(data: Any, shared: dict[str, Any]) -> Any:"
    },
    {
        "problem_type": "locking_contention",
        "language": "python",
        "name": "61_locking_contention_parll_hist",
        "parallelism_model": "charm4py",
        "prompt": "import charm4py\n\nBuild a histogram with `bins` buckets from the sample values in `data`.\n   Use charm4py to compute in parallel.  Assume charm4py has already been initialized.\n   Example:\n\n   input:\n      data = [0.12, 0.76, 0.54, 0.03, 0.33, 0.90, 0.61, 0.28]\n      bins = 4\n\n   output:\n      histogram = [2, 3, 1, 2]\n\nstd::list<std::uint64_t> parallel_histogram(\n    std::list<double> const& data,\n    std::size_t bins);"
    },
    {
        "problem_type": "locking_contention",
        "language": "python",
        "name": "62_locking_contention_bfs",
        "parallelism_model": "charm4py",
        "prompt": "import charm4py\n\ndef add_edge(u: int, v: int) -> None:\n        adjacency[u].push_back(v);\n        adjacency[v].push_back(u);\n    }\n\n    std::size_t size() const noexcept\n    {\n        return adjacency.size();\n    }\n};\n   Perform a level-synchronous breadth-first search starting from `source`\n   and return a list whose ith entry is the number of vertices in level i.\n   The graph is represented as an adjacency list (`Graph`) of undirected edges.\n   Use charm4py to compute in parallel.  Assume charm4py has already been initialized.\n\n   Example:\n\n   input:\n      Graph: 0--1--2\n             |  |  |\n             3--4--5\n      source = 0\n\n   output:\n      level counts = [1, 2, 3]\nstd::list<std::size_t> bfs_next_level_counts(Graph const& g, int source){"
    },
    {
        "problem_type": "locking_contention",
        "language": "python",
        "name": "63_locking_contention_Astar",
        "parallelism_model": "charm4py",
        "prompt": "import charm4py\n\nusing Graph = std::list<std::list<Edge>>;\n\n   Run an A* search on `graph` from `start` to `goal` using `heuristic`.\n   Return an AStarResult containing the path (sequence of vertex ids),\n   its total cost, and a flag indicating success.\n   The graph is represented as an adjacency list (`Graph`) of undirected edges.\nUse charm4py to compute in parallel.  Assume charm4py has already been initialized.\n\n   Example:\nGraph graph = {\n  '''\n0\n''' { Edge{1, 1.0}, Edge{3, 2.5} },\n  '''\n1\n''' { Edge{0, 1.0}, Edge{2, 1.0} },\n  '''\n2\n''' { Edge{1, 1.0}, Edge{5, 1.2} },\n  '''\n3\n''' { Edge{0, 2.5}, Edge{4, 0.8} },\n  '''\n4\n''' { Edge{3, 0.8}, Edge{5, 0.4} },\n  '''\n5\n''' { Edge{2, 1.2}, Edge{4, 0.4} }\n};\n\nstd::size_t const start = 0;\nstd::size_t const goal  = 5;\n\nstd::list<double> heuristic = {\n  '''\n0\n''' 3.2,  // straight-line estimate from vertex i to goal\n  '''\n1\n''' 2.2,\n  '''\n2\n''' 1.2,\n  '''\n3\n''' 1.5,\n  '''\n4\n''' 0.4,\n  '''\n5\n''' 0.0   // goal\n};\n\n   output:\n      path       = [0, 1, 2, 5]\n      total_cost = 3.2\n      found      = true\n\ndef parallel_astar(graph: dict[str, Any], start: Any, goal: Any, heuristic: list[float]) -> Any:"
    },
    {
        "problem_type": "locking_contention",
        "language": "python",
        "name": "64_locking_contention_lockingmat",
        "parallelism_model": "charm4py",
        "prompt": "import charm4py\n\ntemplate<typename GO, typename SC>\n};\ntemplate<typename MatrixType>\nclass LockingMatrix {\npublic:\n  using spinlock = hpx::lcos::local::spinlock;\n\n  explicit LockingMatrix(MatrixType& matrix)\n    : matrix_(matrix)\n  {\n    // allocate per-entry unique_ptr<spinlock> to match matrix_.row_indices shape\n    row_locks_.reserve(matrix_.row_indices.size());\n    for (std::size_t r = 0; r < matrix_.row_indices.size(); ++r) {\n      std::size_t cols = matrix_.row_indices[r].size();\n      row_locks_.emplace_back();\n      row_locks_.back().reserve(cols);\n      for (std::size_t c = 0; c < cols; ++c) {\n        row_locks_.back().push_back(std::make_unique<spinlock>());\n      }\n    }\n  }\n  MatrixType& matrix_;\n  // per-row array of per-entry locks (unique_ptr to avoid non-movable spinlock issues)\n  std::list<std::list<std::unique_ptr<spinlock>>> row_locks_;\n};\nAccumulate contributions from multiple sparse input lists into one row of\n   a sparse matrix concurrently.\nUse charm4py to compute in parallel.  Assume charm4py has already been initialized.\n\n   Example:\n\n   input:\n      row_indices = [0, 1, 2, 3]\n      row_coefs   = [0.0, 0.0, 0.0, 0.0]\n\n      in1_idx  = [1, 3]\n      in1_coef = [1.5, 2.5]\n\n      in2_idx  = [0, 2]\n      in2_coef = [0.5, 3.0]\n\n   Exmplanation of computation:\n      entry 0: 0.0 + 0.5 = 0.5   (from in2)\n      entry 1: 0.0 + 1.5 = 1.5   (from in1)\n      entry 2: 0.0 + 3.0 = 3.0   (from in2)\n      entry 3: 0.0 + 2.5 = 2.5   (from in1)\n\n   output:\n      updated_row_coefs = [0.5, 1.5, 3.0, 2.5]\n\nusing Matrix = SimpleMatrix<int, double>;\nMatrix matrix;\nLockingMatrix<Matrix> lm(matrix);\ntemplate<typename LM, typename MatrixT, typename GO = int, typename SC = double>\nstd::list<SC> locking_mat_add(\n    LM& lm,\n    MatrixT& matrix,\n    GO target_row,\n    const std::list<GO>& in1_idx,\n    const std::list<SC>& in1_coef,\n    const std::list<GO>& in2_idx,\n    const std::list<SC>& in2_coef){"
    }
]